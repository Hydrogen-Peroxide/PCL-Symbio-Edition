<local:MyPageLeft x:Class="PCL.PageBedrockLeft"
                  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                  xmlns:local="clr-namespace:PCL"
                  mc:Ignorable="d" x:Name="PanBack"
                  d:DesignHeight="417.2" Width="300">
    <Grid Name="PanInput" RenderTransformOrigin="0.5,0.5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="20" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="20" />
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="10" />
        </Grid.ColumnDefinitions>
        <Grid.RenderTransform>
            <ScaleTransform />
        </Grid.RenderTransform>

        <!-- 已安装实例列表标题 -->
        <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="5"
                   Text="已安装的基岩版" FontSize="14" FontWeight="Bold"
                   Foreground="{DynamicResource ColorBrush3}"
                   Margin="20,15,20,5" />

        <!-- 实例列表 -->
        <ListBox x:Name="ListInstances" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="5"
                 Margin="20,0" MinHeight="60" MaxHeight="200"
                 SelectionChanged="ListInstances_SelectionChanged"
                 DisplayMemberPath="VersionName" />

        <!-- 启动按钮 -->
        <Grid Grid.ColumnSpan="5" Grid.Row="3" RenderTransformOrigin="0.5,0.5" Margin="0,10,0,0">
            <local:MyButton x:Name="BtnLaunch" Height="54" Margin="20,0" Text="启动基岩版" ColorType="Highlight"
                            IsEnabled="False" Click="BtnLaunch_Click" Padding="30,0,30,15" />
            <TextBlock x:Name="LabVersion" Text="未选择版本" Margin="35,0,35,10" IsHitTestVisible="False"
                       VerticalAlignment="Bottom" HorizontalAlignment="Center" TextTrimming="CharacterEllipsis"
                       FontSize="11" Foreground="{DynamicResource ColorBrushGray3}" />
        </Grid>

        <!-- 返回按钮 -->
        <local:MyButton Grid.Row="4" Grid.Column="2" x:Name="BtnBack" Height="35" Margin="0,10,10,0"
                        Text="返回 Java 版" Click="BtnBack_Click" />
    </Grid>
</local:MyPageLeft>